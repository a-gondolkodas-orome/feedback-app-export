extends ../layout

block content
  a(id='navigate', href='..') Vissza
  br
  br

  h1= "Események"

  table
    thead
    tbody
      each event in events
        tr
          td(class='click')
            a(id='entry', href='./'+event.id+'/')= event.id
